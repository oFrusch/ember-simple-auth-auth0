<h3>
  You are currently logged as: {{this.session.data.authenticated.profile.email}}
  <img src="{{this.session.data.authenticated.profile.picture}}" width="50" height="50">
</h3>
<h3>
  {{#if this.session.data.authenticated.profile.impersonated}}
    Impersonated by: {{this.session.data.authenticated.profile.impersonator.email}}
  {{/if}}
</h3>
<div>
  Token expires {{moment-calendar this.expiresIn}} {{moment-from-now this.expiresIn}}
</div>
<a href="#" {{action "logout"}}>
  {{#if this.session.data.authenticated.profile.impersonated}}
    Stop Impersonating
  {{else}}
    Logout of Ember Simple Auth
  {{/if}}
</a>
<br/>
<a href="#" {{on "click" this.logoutOfAuth0}}>
  Logout of Auth0 Session
</a>

<h2>Example of protected route that requires the user to be authenticated.</h2>
<h3>Secret code is 12345</h3>
<br/>

<h2>Here are some protected posts</h2>

{{#each this.posts as |post|}}
  {{post.title}}
{{/each}}